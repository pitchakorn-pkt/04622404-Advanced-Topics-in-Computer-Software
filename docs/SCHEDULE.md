# SCHEDULE.md — ใครทำอะไร วันไหน · โครงงาน **ช่วยด้วย (ChuayDuay)**

> ส่งให้ทุกคน · อ่านคู่กับไฟล์งานของตัวเอง
> **กฎข้อเดียวที่สำคัญที่สุด: ไม่มีใครต้องหยุดรอใคร** ระหว่างที่ของคนอื่นยังไม่เสร็จ ให้เรียก stub ของเขา (หัวหน้าทำ stub ให้ทุกตัวตั้งแต่ D1 และ stub ตอบตาม `CONTRACT.md` เป๊ะ)

## ปฏิทิน

| วัน | วันที่ | ชื่อรอบ |
|---|---|---|
| D1 | พุธ 16 ก.ย. | หัวหน้าตั้ง repo · ทุกคนอ่านเอกสาร · งานที่ไม่ต้องรอ repo เริ่มได้เลย |
| D2 | พฤ 17 ก.ย. | ทุกคน `make up` ได้ · แทน stub ด้วยโค้ดจริงชิ้นแรก |
| D3 | ศุกร์ 18 ก.ย. | **Must ของตัวเองรันเดี่ยวผ่าน · PR แรกเข้า develop** |
| D4 | เสาร์ 19 ก.ย. | **Integration รอบ 1** — ถาม 1 คำถามผ่านหน้าเว็บจริงแล้วได้คำตอบมีอ้างอิง |
| D5 | อาทิตย์ 20 ก.ย. | แก้บั๊กรอบ 1 · ทำ Should · **จุดตัดสินใจถ้าใครไม่ทัน** |
| D6 | จันทร์ 21 ก.ย. | **Feature freeze 20:00** · รัน eval · merge develop → main |
| D7 | อังคาร 22 ก.ย. | ซ้อมนำเสนอ · ทำสไลด์ · อัดวิดีโอสำรอง |
| **นำเสนอ** | **พุธ 23 ก.ย.** | ทุกคนพูดส่วนตัวเองคนละ 2 นาที |

## ตารางรวม — ดูว่าวันนี้ใครต้องส่งอะไร

| | D1 พุธ | D2 พฤ | D3 ศุกร์ | D4 เสาร์ | D5 อาทิตย์ | D6 จันทร์ | D7 อังคาร |
|---|---|---|---|---|---|---|---|
| **01 Web** | ติดตั้งเครื่องมือ ทำ mock | หน้า login + chat บน mock | ต่อ api จริง · **PR แรก** | แสดง sources + badge ครบ | แผง trace + dashboard | เก็บงาน UI | ซ้อม สาธิตหน้าเว็บ |
| **02 API** | อ่าน contract | **login + chat บน stub เสร็จ** ← 01 รอ | upload + proxy 07 · **PR แรก** | ต่อ router จริง | rate limit + เทส | เก็บงาน | ซ้อม |
| **03 Router** | เขียน 40 เคสทดสอบ | cascade ชั้น 0/1/3 บน stub | **5 route ครบบน stub** · **PR แรก** | **ต่อ 04/05/06 จริง ← ทุกคนรอ** | fallback + trace + rewrite | วัด route accuracy | ซ้อม |
| **04 Engines** | เริ่ม dataset | **`/general` ใช้ได้จริง** ← 03 รอ | **`/local/classify` + ส่ง how-to 10 เรื่องให้ 05** | เทียบโมเดล | ตารางเทียบ 3 วิธี | เก็บตัวเลข | ซ้อม |
| **05 Retrieval** | ขอไฟล์คลังจากหัวหน้า · map golden set | ingest คลังหลัก · index แรกขึ้น | **`/search` ใช้ได้ ← 06 กับ 03 รอ** · **PR แรก** | รับ how-to 20 เรื่อง re-index | วัด hit@5 เทียบ 3 แบบ | เก็บตัวเลข | ซ้อม |
| **06 Generation** | เขียน prompt template + fixtures | grounded บน fixtures | **grounded ใช้ได้จริง ← 03 รอ** · **PR แรก** | 3 mode ครบ + citation check | safety + PII + วัดผล | เก็บตัวเลข | ซ้อม |
| **07 Log** | อ่าน contract | **`/log` + `/history` เสร็จ** ← 02 รอ | feedback + stats · **PR แรก** · **ส่ง how-to 10 เรื่องให้ 05** | ต่อ 02 จริง | สถิติ layer + export | เก็บตัวเลข | ซ้อม |
| **08 หัวหน้า** | **skeleton + stub + compose ← ทุกคนรอ** | ส่งไฟล์คลังให้ 05 · รีวิว PR | รีวิว PR · เตรียม smoke | **คุม integration** | ตัดสินใจถ้าใครไม่ทัน | **freeze + eval + report.html** | คุมซ้อม |

---

## 01 — Web App

| วัน | ต้องเสร็จ |
|---|---|
| D1 | ติดตั้ง Node + clone repo (ถ้าหัวหน้าเสร็จแล้ว) · อ่าน `CONTRACT.md` ข้อ 1 ให้จบ · สร้างโฟลเดอร์ `mocks/` ที่คืน `ChatResponse` ปลอมตาม contract |
| D2 | หน้า login + หน้า chat ทำงานบน mock ได้ครบ (ยังไม่ต้องต่อของจริง) |
| D3 | สลับไปต่อ `api` จริง (02 มี login + chat บน stub ให้แล้วตั้งแต่ D2) · **เปิด PR แรก** |
| D4 | แสดง `sources` เป็นรายการใต้คำตอบ กด `[1]` แล้วเลื่อนไปถูก · badge route แปลงเป็นป้ายไทย · latency |
| D5 | **แผงแสดงการตัดสินใจของ agent** จาก `trace` · ปุ่ม 👍👎 · sidebar session · ปุ่มตัวอย่าง 5 ปุ่ม |
| D6 | dashboard จาก `/api/stats` · จัดการ error ครบ · responsive · **หยุดเพิ่มฟีเจอร์ 20:00** |
| D7 | ซ้อมสาธิต — คุณเป็นคนกดสาธิตหน้าเว็บ ต้องกดได้ลื่นไม่ติดขัด |

**คุณรอใคร**: 02 (แต่ใช้ mock ไปก่อนได้ ไม่ต้องหยุด) · **ใครรอคุณ**: ไม่มี แต่คุณคือหน้าตาตอนสาธิต

---

## 02 — API Backend

| วัน | ต้องเสร็จ |
|---|---|
| D1 | อ่าน `CONTRACT.md` ข้อ 1, 2, 6 · เตรียม Python + FastAPI |
| D2 | **login + `/api/chat` บน stub ของ router ทำงานได้** — ข้อนี้ห้ามเลื่อน เพราะ 01 ต่อของจริงไม่ได้จนกว่าคุณจะมี |
| D3 | `/api/upload` + proxy ไป 07 ครบ 4 เส้น · **เปิด PR แรก** |
| D4 | ต่อ router ตัวจริง · error mapping 502/504 · ส่ง `trace` ต่อให้หน้าเว็บ |
| D5 | rate limit · retry · เทส pytest |
| D6 | เก็บงาน · **หยุดเพิ่มฟีเจอร์ 20:00** |
| D7 | ซ้อม |

**คุณรอใคร**: 03 กับ 07 (ใช้ stub ไปก่อน) · **ใครรอคุณ**: 01 รอคุณตั้งแต่ D2

---

## 03 — AI Router — **critical path ของทีม**

| วัน | ต้องเสร็จ |
|---|---|
| D1 | **เขียน `tests/routing_cases.jsonl` 40 ข้อ ครบทั้ง 5 route** ทำก่อนเขียนโค้ด · ไม่ต้องรอ repo |
| D2 | cascade ชั้น 0 (guard) + ชั้น 1 (rules) + ชั้น 3 (LLM) บน stub · สคริปต์วัด route accuracy |
| D3 | **5 route ทำงานครบบน stub** · **เปิด PR แรก** |
| D4 | **ต่อ 04 / 05 / 06 ตัวจริง ให้เส้น `university_rag` วิ่งครบ** ← ทั้งทีมรอข้อนี้ ห้ามเลื่อน |
| D5 | ชั้น 2 (classifier) · fallback ตอน 05 ล่ม · คืน `trace` · query rewriting |
| D6 | วัด route accuracy บน 40 ข้อ เขียนตัวเลขลง README · **หยุด 20:00** |
| D7 | ซ้อม — เตรียมอธิบายว่าทำไมถึงใช้ cascade พร้อมตัวเลข "ไม่เรียก LLM กี่ %" |

**คุณรอใคร**: 04, 05, 06 (ใช้ stub ไปก่อนได้เต็มที่) · **ใครรอคุณ**: 02 และ integration ทั้งระบบ

---

## 04 — AI Engines

| วัน | ต้องเสร็จ |
|---|---|
| D1 | **เริ่มเขียน `data/intents.csv`** ไม่ต้องรอ repo · ขอไฟล์คลัง 194 ข้อจากคนที่ 5 มาใช้เป็นตัวอย่างตั้งต้น |
| D2 | **`POST /general` ใช้ได้จริง** ← 03 รอ · fallback provider |
| D3 | `train.py` + **`POST /local/classify` ใช้ได้** · **เปิด PR แรก** · **ส่งบทความ how-to 10 เรื่องให้คนที่ 5** |
| D4 | ปรับ dataset จากคำที่ classifier พลาด · token budget |
| D5 | เทียบ TF-IDF vs embedding vs ถาม LLM ทำตาราง 3 แถว |
| D6 | เขียน accuracy ลง README · **หยุด 20:00** |
| D7 | ซ้อม — เตรียมอธิบายว่าทำไมต้องมีทั้ง General AI และ Local AI |

**คุณรอใคร**: ไม่รอใครเลย เริ่มได้ทันที · **ใครรอคุณ**: 03 (D2 `/general`, D3 `/classify`) และ 05 (D3 บทความ)

---

## 05 — Retrieval — **หนักที่สุดเชิงเวลา เริ่มวันนี้**

| วัน | ต้องเสร็จ |
|---|---|
| D1 | **ขอ 2 ไฟล์จากหัวหน้า** (`daily_tech_qa.txt`, `golden_set.json`) · อ่านข้อควรระวัง 3 ข้อในไฟล์งานตัวเอง · **เริ่ม map `relevant_chunk_ids` ก่อนอย่างอื่น** |
| D2 | `ingest.py` รันได้ · index แรกขึ้น (รวมคู่ถาม-ตอบเป็นเอกสารก่อนแล้วค่อย chunk) |
| D3 | **`POST /search` ใช้ได้จริง** ← 06 กับ 03 รอ · **เปิด PR แรก** |
| D4 | รับบทความ how-to 20 เรื่องจาก 04 และ 07 · ตรวจคุณภาพ · re-index |
| D5 | `eval_retrieval.py` วัด hit@1 / hit@5 / MRR เทียบ BM25 / vector / hybrid **ด้วย variant `paraphrase`** |
| D6 | ตาราง 3 แถวลง README · **หยุด 20:00** |
| D7 | ซ้อม — เตรียมอธิบายว่าทำไมต้อง hybrid พร้อมตาราง |

**คุณรอใคร**: หัวหน้าส่งไฟล์ (D1–D2) · **ใครรอคุณ**: 06 และ 03 ตั้งแต่ D3 · ทั้งทีมรอ golden set

---

## 06 — LLM Generation

| วัน | ต้องเสร็จ |
|---|---|
| D1 | เขียน `prompts/grounded.j2` + `tests/fixtures/` 5 ชุด เขียน contexts ตัวอย่างเอง ไม่ต้องรอ 05 |
| D2 | mode `grounded` ทำงานบน fixtures ได้ |
| D3 | **`grounded` ใช้ได้จริง** ← 03 รอ · **เปิด PR แรก** |
| D4 | 3 mode ครบ · **citation verification** ตัดเลข `[n]` ที่ไม่มีจริงออก |
| D5 | safety + PII regex · fallback provider · สคริปต์วัด % citation ถูก |
| D6 | เขียนตัวเลขลง README · **หยุด 20:00** |
| D7 | ซ้อม — เตรียมโชว์ prompt template บนสไลด์ |

**คุณรอใคร**: 05 (ใช้ fixtures ไปก่อน) · **ใครรอคุณ**: 03 ตั้งแต่ D3

---

## 07 — Response / Log

| วัน | ต้องเสร็จ |
|---|---|
| D1 | อ่าน `CONTRACT.md` ข้อ 6 · อ่าน "สี่กับดัก" ในไฟล์งานตัวเองให้จบก่อนเขียนโค้ด |
| D2 | **สร้าง 3 ตาราง + `POST /log` + `GET /history` เสร็จ** ← 02 รอ · ทดสอบด้วย curl ยิงข้อมูลปลอมได้เลย ไม่ต้องรอใคร |
| D3 | `/feedback` + `/sessions` + `/stats` · **เปิด PR แรก** · **ส่งบทความ how-to 10 เรื่องให้คนที่ 5** |
| D4 | ต่อ 02 ตัวจริง · ตกลงหน้าตา `/stats` กับคนที่ 1 |
| D5 | เก็บ `decided_at_layer` ลงคอลัมน์แยก · สถิติ layer · export CSV |
| D6 | ตรวจตัวเลข `/stats` ตรงกับข้อมูลจริง · **หยุด 20:00** |
| D7 | ซ้อม |

**คุณรอใคร**: ไม่รอใครเลย · **ใครรอคุณ**: 02 ตั้งแต่ D2, 01 รอ `/stats`, 05 รอบทความ D3

---

## 08 — หัวหน้า

| วัน | ต้องเสร็จ |
|---|---|
| D1 | **repo + skeleton + stub ทุก service + compose + Makefile + smoke** ← ทุกคนรอ ต้องเสร็จก่อนสิ้นวัน · สร้าง branch ให้ครบ 7 คน · ส่งไฟล์งานให้ทุกคน |
| D2 | **ส่ง 2 ไฟล์คลังให้คนที่ 5** · รีวิว PR · ช่วยคนที่ติด docker |
| D3 | รีวิว PR แรกของทุกคน · เตรียม `make warmup` |
| D4 | **คุม integration รอบ 1** ไล่ `request_id` ทีละ hop · เจอบั๊กเปิด Issue ไม่แก้เอง |
| D5 | **จุดตัดสินใจถ้าใครไม่ทัน** (ยุบ 07 เข้า 02 ได้ · ห้ามยุบ 03/05) |
| D6 | **freeze 20:00** → รัน eval → `eval/report.html` → merge develop → main → tag |
| D7 | คุมซ้อม · อัดวิดีโอสำรอง · เช็ก `git shortlog` ว่าทุกคนมี commit |

---

## ถ้าตามไม่ทัน ตัดอะไรได้บ้าง

ตัดจากล่างขึ้นบน **ห้ามตัดจากบนลงล่าง**

| ลำดับ | สิ่งที่ต้องมี | ตัดได้ไหม |
|---|---|---|
| 1 | `make up` แล้วทั้งระบบขึ้น | **ไม่ได้** |
| 2 | ถาม 1 คำถามแล้วได้คำตอบพร้อมอ้างอิงผ่านหน้าเว็บ | **ไม่ได้** |
| 3 | 5 route ทำงานครบ | ไม่ได้ |
| 4 | history + follow-up + feedback 👍👎 | ไม่ได้ |
| 5 | ตัวเลขวัดผลใน `eval/report.html` | ไม่ควรตัด เป็นจุดขาย |
| 6 | dashboard, upload ไฟล์, แผง trace | ตัดได้ถ้าจำเป็น |
| 7 | streaming, reranker, Grafana, CI | ตัดได้ทันที |
