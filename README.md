# DL-03-Retrieval-Augmented Generation (RAG) — คู่มืออาหารไทย

Build a simple RAG retrieval pipeline from scratch using Python, including data loading, chunking, embeddings, a vector database, and semantic document search. ฐานความรู้ในโปรเจกต์นี้เป็น "คู่มืออาหารไทย 15 เมนู" ในรูปแบบถาม-ตอบ (Q&A)

----
# Structure

```text
RAG-Project/
│
├── data/                                   # Stores the original documents
│   ├── thai_food_qa.txt                    # build the knowledge base (Q&A อาหารไทย 15 เมนู)
│   └── sample_questions.txt                # Sample questions for testing
│
├── outputs/                                # Stores outputs generated by each lab
│   ├── extracted_text.json                 # Extracted Q&A text with line numbers
│   ├── chunks.json                         # Text chunks with metadata
│   ├── embeddings.npy                      # Embedding vectors
│   └── retrieval_results.json              # Top-k retrieval results
│
├── vector_db/                              # Stores the vector database
│   ├── document.index                      # FAISS vector index
│   └── chunk_store.json                    # Chunks and metadata
│
├── labs/                                   # Step-by-step lab scripts
│   ├── lab01_extract_text.py               # Extract Q&A pairs from the .txt
│   ├── lab02_chunking.py                   # Split text into chunks
│   ├── lab03_create_embeddings.py          # Generate embeddings
│   ├── lab04_create_vector_db.py           # Build the FAISS vector database
│   ├── lab05_query_embedding.py            # Create query embeddings
│   ├── lab06_similarity_search.py          # Retrieve Top-k relevant chunks
│   └── lab07_complete_retrieval.py         # Complete retrieval pipeline
│
├── src/                                    # Reusable core modules
│   ├── __init__.py                         # Python package initialization
│   ├── document_loader.py                  # Q&A text loading and extraction
│   ├── text_splitter.py                    # Text chunking
│   ├── embedding_model.py                  # Embedding model
│   ├── vector_store.py                     # FAISS vector database
│   └── retriever.py                        # Semantic retrieval
│
├── config.py                               # Project configuration
├── requirements.txt                        # Required Python packages
└── main.py                                 # Run the retrieval system
```
# Summary

A simple RAG project for learning the basics. It includes step-by-step code for data loading, text chunking, embeddings, vector database creation, and document retrieval. ฐานความรู้เป็นคู่มืออาหารไทย 15 เมนู (เช่น ต้มยำกุ้ง ผัดไทย ส้มตำไทย แกงเขียวหวานไก่ มัสมั่นเนื้อ ข้าวซอย ลาบหมู ข้าวเหนียวมะม่วง) แต่ละเมนูมีคำถาม-คำตอบเรื่องที่มา วัตถุดิบ รสชาติ ระดับความเผ็ด วิธีทำ และเคล็ดลับ This project focuses on understanding the RAG workflow and will continue to be improved with more features.
